# Token Chat Windows — 产品需求文档

## 1. 产品概述

Token Chat Windows 是一款面向 Windows 平台的原生 AI 聊天客户端，支持多个 OpenAI Compatible API Provider，提供完整的多轮对话、Token 统计与费用追踪能力。产品定位为「轻量、专业、可控」的 AI 聊天工具，区别于官方客户端的封闭体验，强调用户对 Provider、模型和费用的完全掌控。

### 1.1 目标用户

- 需要管理多个 API Provider 的开发者和重度用户
- 关注 Token 消耗与费用的个人用户
- 需要对比不同模型输出质量的评测人员
- 习惯 Windows 原生桌面工作流的用户

### 1.2 核心价值

- 多 Provider 统一管理，一个客户端接入所有 OpenAI Compatible 服务
- 精确的 Token 用量追踪与费用计算，每次生成都有完整指标
- 对话分支与历史管理，支持编辑、重生成、归档和搜索
- 本地优先，数据存储在本地 SQLite，API Key 使用系统安全存储

### 1.3 协议支持

仅支持 OpenAI Compatible `/chat/completions` 接口。不直接支持 Anthropic 或 Gemini 原生协议，它们需要通过 OpenAI Compatible 网关接入。

---

## 2. 功能需求

### 2.1 Provider 管理

#### 2.1.1 Provider 配置

用户可以创建、编辑和删除多个 API Provider。每个 Provider 包含以下字段：

| 字段 | 说明 |
|------|------|
| 名称 | 用户自定义的显示名称，如 "OpenAI"、"DeepSeek" |
| Base URL | API 端点地址，如 `https://api.openai.com/v1` |
| API Key | 认证密钥，使用 Windows Credential Manager 安全存储 |

#### 2.1.2 模型配置

每个 Provider 下可配置多个模型。每个模型包含以下字段：

| 字段 | 说明 |
|------|------|
| 显示名称 | 用户自定义的模型显示名，如 "GPT 4.1 Mini" |
| API 模型名称 | 发送给 API 的模型标识符，如 `gpt-4.1-mini` |
| System Prompt | 该模型的默认系统提示词 |
| Temperature | 生成温度，范围 0.0–2.0 |
| 最大输出 Token | 可选，限制单次生成的最大输出长度 |
| 上下文窗口 | 模型的上下文窗口大小（Token 数） |
| 工具定义 | OpenAI Compatible 格式的 tools JSON 数组 |
| 缓存外输入单价 | 每百万 Token 的未缓存输入价格 |
| 缓存内读取单价 | 每百万 Token 的缓存命中价格 |
| 输出单价 | 每百万 Token 的输出价格 |
| 货币代码 | 价格货币，如 USD、CNY |

#### 2.1.3 连通性测试

保存 Provider 前可发起测试请求，验证 API Key 和 URL 是否有效。结果需显示：

- 成功/失败状态
- 延迟毫秒数
- 失败原因（认证失败、网络不可达、URL 格式错误等）

#### 2.1.4 模型自动发现

调用 `/v1/models` 接口自动获取 Provider 下可用的模型列表，用户从中选择要启用的模型，无需手动输入模型名称。

#### 2.1.5 Provider 健康监控

记录每个 Provider 的：

- 历史成功率（成功请求 / 总请求）
- 平均延迟
- 最近一次错误信息

在 Provider 列表中展示健康状态摘要。

#### 2.1.6 配置导入与导出

- 导出：生成 JSON 文件，包含 Provider 名称、Base URL 和模型配置，**始终排除 API Key**
- 导入：读取 JSON 文件，合并到现有配置。已存在的 Provider 保留其 API Key，新增 Provider 的 API Key 需用户手动填写

---

### 2.2 对话管理

#### 2.2.1 对话列表

侧边栏展示所有对话列表，每个对话项显示：

- 标题（默认"新对话"，支持自动生成）
- 绑定的 Provider / 模型名称
- 最后更新时间

#### 2.2.2 对话搜索

支持按标题和消息内容进行全文搜索，实时过滤对话列表。

#### 2.2.3 对话归档

- 归档：将对话移入归档状态，不在主列表显示
- 取消归档：恢复归档对话到主列表
- 侧边栏提供"显示已归档对话"开关

#### 2.2.4 对话置顶

支持将重要对话置顶，置顶项始终排在列表最前。

#### 2.2.5 对话文件夹与标签

- 支持为对话指定文件夹分组
- 支持为对话添加多个标签（逗号分隔）
- 侧边栏提供按文件夹和标签筛选的功能

#### 2.2.6 对话树

对话支持分支结构（通过编辑消息或重生成创建）。提供对话树可视化界面，展示：

- 根对话与分支对话的层级关系
- 各分支的标题和创建时间
- 点击可跳转到对应对话

#### 2.2.7 对话导出与导入

- **导出为 JSON**：支持导出单个或全部对话，包含完整消息历史和元数据
- **导出为 Markdown**：将当前对话导出为可读的 Markdown 格式
- **导入 JSON**：从 JSON 文件恢复对话历史

---

### 2.3 聊天功能

#### 2.3.1 多轮对话

- 每个对话独立绑定一个 Provider 和模型
- 支持上下文连续的多轮对话
- 每条消息记录其创建时的 Provider 和模型快照，确保历史可追溯

#### 2.3.2 SSE 流式输出

- 使用 Server-Sent Events 实现流式响应
- 支持 Thinking 模型：思考过程与最终回答分别流式展示
- 思考过程默认展开显示，完成后可折叠

#### 2.3.3 停止生成

在生成过程中可随时停止，停止后：

- 已输出的内容保留
- 记录本次生成为 "cancelled" 状态
- 终止 HTTP 请求和流连接

#### 2.3.4 重生成

对助手消息可触发重生成，生成到新分支，原消息保留不变。

#### 2.3.5 消息编辑与分支

用户消息支持编辑，编辑后创建新分支并重新发送，原对话路径保留。

#### 2.3.6 消息复制

- 每条消息右上角提供复制按钮
- 代码块右上角提供独立的复制按钮
- 长按消息弹出操作菜单，包含复制和分享选项

#### 2.3.7 对话内搜索

在当前对话内搜索消息内容，支持匹配：

- 消息正文
- 思考过程
- Provider / 模型名称
- 附件名称
- 工具调用内容

---

### 2.4 附件支持

#### 2.4.1 文件附件

- 支持添加图片和文本文件作为附件
- 单个附件大小限制：5 MB
- 图片附件：以 Base64 编码发送，支持缩略图预览和全屏查看
- 文本文件附件：读取内容为 UTF-8 文本发送
- 发送前可移除已添加的附件

#### 2.4.2 工具调用展示

- 接收并展示 OpenAI Compatible 格式的工具调用
- 工具调用只展示，不在本地执行任意工具
- 展示工具名称、参数和返回结果

---

### 2.5 Token 统计与费用

#### 2.5.1 单次消息指标

每条助手消息固定展示以下指标：

| 指标 | 说明 |
|------|------|
| 首事件延迟 | 从发送到收到第一个 SSE 事件的时间 |
| 首 Token 延迟 | 从发送到收到第一个可展示文本/推理内容的时间 |
| 缓存外输入 Token | 按普通价格计费的输入 Token 数 |
| 缓存内读取 Token | 命中提示缓存的 Token 数 |
| 输出 Token | 生成的输出 Token 数 |
| 总耗时 | 从发送到完成的总时间 |
| 费用 | 根据价格快照计算的本次生成费用 |

#### 2.5.2 对话级汇总

对话顶部展示当前对话的累计统计：

- 缓存外输入 Token 总量
- 缓存内读取 Token 总量
- 输出 Token 总量
- 累计费用
- 实时流速（tokens/秒）和已用时间

#### 2.5.3 模型级聚合

按 Provider / 模型聚合统计：

- 请求数
- 缓存外输入、缓存内读取、输出 Token 总量
- Token 总量
- 总费用

#### 2.5.4 对话级聚合

按对话维度聚合统计，了解每个对话的资源消耗。

#### 2.5.5 生成运行记录

每次生成（包括失败、取消和重试）记录为独立的 generation_run：

- 状态（completed / cancelled / failed）
- Provider Request ID
- 首事件延迟、首 Token 延迟、总耗时
- 各类 Token 数量
- 价格快照与费用明细

#### 2.5.6 时间筛选

统计页面支持三种时间范围筛选：

- 全部
- 今日
- 本月

#### 2.5.7 统计导出

支持将使用统计导出为：

- JSON 格式
- CSV 格式

---

### 2.6 费用预算告警

#### 2.6.1 预算设置

- 启用/禁用费用预算告警
- 设置每日预算上限
- 设置每月预算上限
- 设置告警阈值比例（50%–100%）

#### 2.6.2 告警行为

- 接近阈值时弹出提醒对话框
- 超出预算后可选择：
  - 仅警告：弹出确认对话框，用户可选择继续发送
  - 阻止发送：直接阻止，显示超出预算提示

---

### 2.7 System Prompt 模板

#### 2.7.1 模板管理

- 创建、编辑、删除 System Prompt 模板
- 每个模板包含：名称、分类、Prompt 内容
- 模板列表在设置页面展示

#### 2.7.2 模板套用

在模型编辑页面可通过下拉菜单快速套用已有模板到 System Prompt 字段。

---

### 2.8 多模型对比

#### 2.8.1 对比发起

- 输入对比 Prompt
- 从可用模型列表中勾选多个模型
- 点击"开始对比"同时发送请求

#### 2.8.2 对比结果

- 以卡片形式展示每个模型的回复
- 显示模型名称和回复内容
- 可直接跳转到对应模型生成的对话

---

### 2.9 UI 与主题

#### 2.9.1 主题模式

- 暗色主题（默认）
- 亮色主题
- 跟随系统设置自动切换

#### 2.9.2 对话样式自定义

- 气泡颜色方案：蓝色、绿色、低饱和
- 布局密度：紧凑 / 舒适
- 字体大小：0.85x – 1.35x 可调

#### 2.9.3 语言

- 中文界面（默认）
- 英文界面
- 跟随系统语言

---

### 2.10 上下文窗口预算

- 基于中英文混合 Token 估算，计算当前对话的 Token 使用量
- 在输入时实时显示已用 Token / 上下文窗口总量
- 接近上下文窗口上限时给出警告

---

### 2.11 草稿自动保存

- 输入框内容在切换对话后自动保存
- 切回对话时自动恢复未发送的草稿
- 发送后清除对应草稿

---

### 2.12 对话标题自动生成

新建对话后，根据第一条用户消息自动生成有意义的标题，替代默认的"新对话"。

---

## 3. 数据模型

### 3.1 Provider 配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 唯一标识 |
| name | TEXT | 显示名称 |
| base_url | TEXT | API 端点地址 |
| secret_ref | TEXT | 安全存储中的 API Key 引用 |
| extra_headers_json | TEXT | 额外请求头 JSON |
| created_at | INTEGER | 创建时间戳 |
| updated_at | INTEGER | 更新时间戳 |

### 3.2 模型配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 唯一标识 |
| provider_id | TEXT | 所属 Provider ID |
| model_name | TEXT | API 模型名称 |
| display_name | TEXT | 显示名称 |
| context_window | INTEGER | 上下文窗口大小 |
| uncached_input_nanos_per_million | INTEGER | 缓存外输入单价（纳米单位） |
| cache_read_nanos_per_million | INTEGER | 缓存内读取单价 |
| cache_write_nanos_per_million | INTEGER | 缓存写入单价 |
| output_nanos_per_million | INTEGER | 输出单价 |
| currency | TEXT | 货币代码 |
| capabilities_json | TEXT | 模型能力 JSON |

### 3.3 对话表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 唯一标识 |
| title | TEXT | 对话标题 |
| provider_id | TEXT | 绑定的 Provider ID |
| model_id | TEXT | 绑定的模型 ID |
| parent_conversation_id | TEXT | 父对话 ID（分支时设置） |
| created_at | INTEGER | 创建时间戳 |
| updated_at | INTEGER | 更新时间戳 |
| archived_at | INTEGER | 归档时间戳（null 表示未归档） |

### 3.4 消息表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 唯一标识 |
| conversation_id | TEXT | 所属对话 ID |
| parent_message_id | TEXT | 父消息 ID（分支时设置） |
| role | TEXT | 角色：user / assistant / system |
| content_json | TEXT | 消息内容 JSON |
| reasoning_content | TEXT | 思考过程内容 |
| provider_id | TEXT | 请求时的 Provider ID 快照 |
| provider_name | TEXT | 请求时的 Provider 名称快照 |
| model_id | TEXT | 请求时的模型 ID 快照 |
| model_name | TEXT | 请求时的模型名称快照 |
| status | TEXT | 状态：completed / streaming / cancelled / failed |
| attachments_json | TEXT | 附件列表 JSON |
| tool_calls_json | TEXT | 工具调用列表 JSON |
| error | TEXT | 错误信息 |
| created_at | INTEGER | 创建时间戳 |

### 3.5 生成运行记录表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 唯一标识 |
| conversation_id | TEXT | 所属对话 ID |
| assistant_message_id | TEXT | 关联的助手消息 ID |
| provider_id | TEXT | Provider ID |
| model_id | TEXT | 模型 ID |
| status | TEXT | 状态 |
| provider_request_id | TEXT | 供应商返回的 Request ID |
| uncached_input_tokens | INTEGER | 缓存外输入 Token |
| cache_read_input_tokens | INTEGER | 缓存内读取 Token |
| cache_write_input_tokens | INTEGER | 缓存写入 Token |
| output_tokens | INTEGER | 输出 Token |
| usage_source | TEXT | 来源：provider_reported / estimated |
| first_event_latency_ms | INTEGER | 首事件延迟 |
| first_token_latency_ms | INTEGER | 首 Token 延迟 |
| duration_ms | INTEGER | 总耗时 |
| price_snapshot_json | TEXT | 价格快照 |
| cost_nanos | INTEGER | 费用（纳米单位） |
| currency | TEXT | 货币代码 |
| error_json | TEXT | 错误详情 JSON |
| created_at | INTEGER | 创建时间戳 |

---

## 4. 安全需求

### 4.1 API Key 存储

- API Key 使用 Windows Credential Manager 存储，不明文写入数据库或配置文件
- 导入/导出 Provider 配置时始终排除 API Key
- API Key 仅在发起请求时短暂加载到内存，使用后尽快清除

### 4.2 网络安全

- 默认只允许 HTTPS 连接
- 用户启用 HTTP 时显示明确警告
- 禁止跨域重定向携带认证 Header
- 禁止记录 Authorization、API Key、自定义敏感 Header 和完整请求体到日志

### 4.3 数据安全

- 所有数据存储在本地 SQLite 数据库
- 无云同步功能，数据完全在用户设备上
- 数据库迁移由单一模块负责，保证历史计价数据一致性

---

## 5. 可靠性需求

### 5.1 请求重试

- 429（限流）和 5xx 服务端错误时自动重试
- 已开始输出后默认不自动重试
- 重试次数有限制

### 5.2 超时控制

每个 Provider 可配置：

- 连接超时
- 首 Token 超时
- 总超时

### 5.3 取消处理

取消生成时必须同时：

- 终止 HTTP 请求
- 关闭 SSE 流连接
- 持久化 cancelled 状态到数据库

### 5.4 价格快照

每次生成运行必须保存价格快照。用户之后修改模型价格时，只影响新请求，历史费用不受影响。

---

## 6. 非功能需求

### 6.1 性能

- 对话列表和消息列表滚动流畅，无卡顿
- 流式输出实时更新，无明显延迟
- 数据库查询响应时间 < 100ms

### 6.2 可用性

- 首次启动引导用户配置至少一个 Provider
- 空状态显示引导文案
- 错误信息清晰可操作

### 6.3 兼容性

- 支持 Windows 10 及以上版本
- 支持高 DPI 显示
- 支持窗口缩放

### 6.4 安装与分发

- 提供标准 Windows 安装程序（MSI 或 Inno Setup）
- 支持静默安装参数

---

## 7. 页面结构

### 7.1 主界面

```
┌──────────────────────────────────────────────────────┐
│ 侧边栏（对话列表）  │  主聊天区域                     │
│                     │  ┌────────────────────────────┐ │
│ [搜索框]            │  │ 对话标题 / 模型信息          │ │
│ [归档开关]          │  │ 对话级统计摘要               │ │
│ [文件夹/标签筛选]    │  ├────────────────────────────┤ │
│                     │  │ 消息列表                    │ │
│ 对话1              │  │  ┌──────────────────────┐   │ │
│ 对话2（置顶）       │  │  │ 用户消息              │   │ │
│ 对话3              │  │  └──────────────────────┘   │ │
│ ...                │  │  ┌──────────────────────┐   │ │
│                     │  │  │ 助手消息 + 指标       │   │ │
│ [新建对话]          │  │  └──────────────────────┘   │ │
│ [导入对话]          │  ├────────────────────────────┤ │
│                     │  │ 输入框 + 附件 + 发送        │ │
└──────────────────────────────────────────────────────┘
```

### 7.2 顶部工具栏

从左到右依次为：

- 搜索当前对话
- 多模型对比
- 设置当前对话模型
- Token 与运行统计
- API Provider 管理
- 设置

### 7.3 设置页面

- 主题模式选择
- 语言选择
- 对话样式（气泡颜色、密度、字体大小）
- 费用预算告警配置
- System Prompt 模板管理

### 7.4 Provider 管理页面

- Provider 列表（含健康状态）
- 添加 / 编辑 / 删除 Provider
- 模型列表（含价格信息）
- 添加 / 编辑 / 删除模型
- 测试连通性
- 自动发现模型
- 导入 / 导出配置

### 7.5 统计页面

- 时间范围筛选（全部 / 今日 / 本月）
- 按模型聚合统计
- 按对话聚合统计
- 每次生成记录下钻
- 导出为 JSON / CSV

---

## 8. 交互规范

### 8.1 消息气泡

- 用户消息：右对齐
- 助手消息：左对齐
- 每条消息显示角色图标、Provider / 模型名称、复制按钮、状态标签
- 助手消息底部展示指标面板

### 8.2 输入区域

- 多行文本输入框
- 附件按钮（点击选择文件）
- 发送按钮 / 停止按钮（生成中切换）
- 附件预览（可移除）

### 8.3 对话列表

- 搜索框实时过滤
- 置顶对话排在最前
- 支持文件夹和标签筛选
- 每个对话项显示标题、模型、时间
- 支持右键菜单（归档、删除等）

### 8.4 代码块

- 深色背景卡片
- 顶部显示语言名称
- 右上角复制按钮
- 基础语法高亮（关键字、字符串、注释）

### 8.5 思考过程面板

- 可折叠的面板
- 流式输出时自动展开
- 完成后可折叠
- 显示字符数统计

### 8.6 工具调用面板

- 可折叠展示
- 显示工具名称和参数
- 标注"仅展示，不会自动执行"
- 支持展开查看完整 JSON
